stage 1
# Building a pipeline: 

1. Create a GitHub repo and clone it in local(Add experiments).
2. Add src folder along with all components (run them individually)
 > components: data_ingestion , data_preporocessing 
3. Add data , models , reports directories to .gitignore file ,bcz we do not want the git to track these artifacts or the metadata , bcz many time it may happen that the size of these data could be very large , eg : if we are using a neural network model then it may happen that its pkl file would go in Gbs. and for this reason only we use git 
4. Now git add , commit , push


stage  2:
# Setting/Automating the dvc pipeline(without params)

5.Create dvc.yaml file and add stages to it.
6. dvc init then do "dvc repro"(command to reproduce the pipeline but for using it first we need to initialize the dvc) to test the pipeline automation . iska itna understanding hai ki next time run hone par ye khud hi kuch steps ko skip kar deta hai ,  where no changes have been made, bcz output will be same  .
(check dvc dag) to visualize the pipeline dag : directed acycling graph

7.Now git add , commit , push

To enable auto staging, run:
dvc config core.autostage true

stage3:
# Setting up dvc pipeline(with params) # because baad mae pura pipeline bana lene kae baad yesa feel hoga ki ye param change kar kae dhekte tho kya result aata .And since there could be a lot of params in a lot of different files and which param is giving us what result is unknown to us , so it is better that whatever configurable params or variables we have , we write it in a configurable file , so that we can change everything in a single place  # this is a industry grade practice. 
8. add params.yaml file
9. add the params setup(mentioned below)
10. Do "dvc repro" again to test the pipeline along with the params
and now that we have one more functionality so we can now do : 
11. Now git add , commit , push

-------------------------------------------------------------------------------

params.yaml setup:
1> import yaml
2> add func:
def load_params(params_path: str) -> dict:
    """Load parameters from a YAML file."""
    try:
        with open(params_path, 'r') as file:
            params = yaml.safe_load(file)
        logger.debug('Parameters retrieved from %s', params_path)
        return params
    except FileNotFoundError:
        logger.error('File not found: %s', params_path)
        raise
    except yaml.YAMLError as e:
        logger.error('YAML error: %s', e)
        raise
    except Exception as e:
        logger.error('Unexpected error: %s', e)
        raise
3> Add to main():

# data_ingestion
#initiating load_params
params = load_params(params_path='params.yaml')
test_size = params['data_ingestion']['test_size']

# feature_engineering
params = load_params(params_path='params.yaml')
max_features = params['feature_engineering']['max_features']

# model_building
params = load_params('params.yaml')['model_building']

-------------------------------------------------------------------------------

#stage4 
Experiments with dvc:
# we did the params tuining but how do we keep track of that with what values what accuracy or output we have achieved . tools help us compare that in multiple experiments comparision what result have we achieved , and with that specific params we can reproduce our pipeline 

12.pip install dvc live 
13.Add the dvclive code block(mentioned below)
14> Do "dvc exp run", it will create a new dvc.yaml(if already not there) and dvclive directory (each run will be considered as an experiment by DVC), it gives a unique name to all the experiments
15> Do "dvc exp show" on terminal to see the experiments or use extension on VSCode (install dvc extension)
16> Do "dvc exp remove {exp-name}" to remove exp (optional) | "dvc exp apply {exp-name}" to reproduce prev exp . and it will not even run the pipeline bcz anyways it has saved the changs , so it just reproduces 
17> Change params, re-run code (produce new experiments)
# now we have added new unctionality so 
18> Now git add, commit, push

-------------------------------------------------------------------------------

dvclive code block: we will do it in model evaluation , bcz after evaluation only we will get what fits best 
1> import dvclive and yaml :
from dvclive import Live
import yaml
2> Add the load_params function and initiate "params" var in main
3> Add below code block to main:
with Live(save_dvc_exp=True) as live:
    live.log_metric('accuracy', accuracy_score(y_test, y_test))
    live.log_metric('precision', precision_score(y_test, y_test))
    live.log_metric('recall', recall_score(y_test, y_test))

    live.log_params(params)

-----------------------------------------------------------------------
#stage5
Adding a remote S3 storage

19. Adding login to AWS console
20. Create an IAM user (straight forward process)
21. Create S3 (enter unique name and create)
22. pip install dvc[s3] # helps dvc to connect to s3 
23. pip install awscli 
24. "aws configure" - on terminal  , it will ask access key id and secret key , if you want to use the previoussly used id then it will come automatically , just hit enter 
25. dvc remote add -d dvcstore s3://bucketname # anyname you want in place of dvcstore
26.dvc commit -push the exp outcome that you want to keep , bcz t is not good to push all your poor experiments into s3 , reproduce only those which are good experiments and push only them
27. finally git add, commit and push